import React from 'react';
import Form from './Form';

function Book() {
  return (
    <div>
    <h2>Books List</h2>
    <p>This list is empty for now!</p>
    <Form />
    </div>
    );
}

export default Book;
